import React from 'react';

import Button from '../../../../../components/Button';

import styles from './index.module.scss';

const Expand = () => {
	return <Button className={styles['button']} name={Expand.name}/>;
};

export default Expand;
